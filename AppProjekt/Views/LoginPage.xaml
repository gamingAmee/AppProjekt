<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:AppProjekt.ViewModels" xmlns:converters="clr-namespace:AppProjekt.Converter"
             x:DataType="viewmodels:LoginViewModel"
             Shell.NavBarIsVisible="False"
             x:Class="AppProjekt.Views.LoginPage">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.BindingContext>
        <viewmodels:LoginViewModel />
    </ContentPage.BindingContext>
    
    <ScrollView>
        <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
            <Button 
                Margin="0,10,0,0" 
                Text="Sign In"
                Command="{Binding LoginCommand}"
                IsVisible="{Binding IsLoggedIn,  Converter={StaticResource InverseBooleanConverter}}"
                TextColor="White" />
            <Button 
                Margin="0,10,0,0" 
                Text="Sign Out"
                Command="{Binding LogoutCommand}"
                BackgroundColor="Red"
                IsVisible="{Binding IsLoggedIn}"
                TextColor="White" />
        </StackLayout>
    </ScrollView>
</ContentPage>